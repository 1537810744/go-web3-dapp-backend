# Go-Web3-DApp Backend (Full)
This is a reproduced full backend for a Go + Ethereum microservices DeFi project.
Services:
- transactor (port 8080)
- indexer   (port 8081)

See config.yaml for settings. Deploy with Docker Compose after filling `ethereum.contract_address` and `dev.faucet_private_key`.

